# Vercel CLI upload excludes (deployment installs deps and builds on the server)
node_modules
.next
out
.vercel
.git
*.log
.env*
.env.local
coverage
.DS_Store

../backend

# Large media — host on CDN or add under 50MB if required on Vercel
*.mov
*.zip
