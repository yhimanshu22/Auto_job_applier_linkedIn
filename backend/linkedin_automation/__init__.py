"""LinkedIn automation package (posting, engagement, calendar, pursuit).

Originally lived as a standalone project at ``Linkedln-Automation-Framework/``;
absorbed into the backend as a regular Python package. Invoke its CLI via
``python -m linkedin_automation <action> ...`` from the backend root.
"""
