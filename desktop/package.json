{
  "name": "linkdapply-desktop",
  "private": true,
  "version": "1.1.1",
  "scripts": {
    "dev": "node scripts/ensure-dev-sidecar.js && tauri dev",
    "build": "node scripts/build-backend.js && node scripts/ensure-dist.js && tauri build",
    "build:tauri": "tauri build",
    "build:backend": "node scripts/build-backend.js",
    "build:backend:force": "node scripts/build-backend.js --force",
    "build:force": "node scripts/build-backend.js --force && node scripts/ensure-dist.js && tauri build",
    "icon": "node scripts/make-icon.js && tauri icon assets/source-icon.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.4.0"
  }
}
